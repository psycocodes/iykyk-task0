package com.iykyk.task0.utils

/**
 * Central Global Configuration Flags.
 */
object AppConfig {
    /**
     * Set to true to enable:
     * 1. Live yellow debug bounding box overlay around detected faces.
     * 2. Verbose ML Kit, TFLite embedding, clustering, and collage logs in Logcat.
     */
    const val DEBUG = true
}
